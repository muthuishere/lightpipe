# Spike log

One line per completed iteration: date · spike · measured result · surprises.

| date | spike | measured | surprise |
|---|---|---|---|
| 2026-08-22 | S0 | SER 0.0, all palettes × cells, ideal channel; P8@8px = 11,781 B/frame | none — clean baseline |
| 2026-08-22 | S1 | potato best clean layer P2@8px 3,927 B/frame; webcam P8@8px 11,781 B/frame | potato reads P8@14px cleanly — L0 floor drafted far too conservative. SER non-monotonic in cell size (sensor-grid aliasing). |
| 2026-08-22 | S2 | false-accept 0/101,000 corrupted frames (1 symbol .. 30% of cells); 25 B header, 2-row band, 1.5-2.7% overhead; P8@8px 11,602 B/frame | the 2-row band re-phased the cell grid against the sensor resample grid — potato P2@8px fell off SER 0 (1.6e-4) while 14/20px stayed clean, so s1-sweep.csv had to be regenerated. Layout changes invalidate the rate ladder. |
| 2026-08-22 | S3 | 3,600 decodes over 0-70% drop x {64K,256K,1M} x {3927,7854,11781}B: mean overhead 0.011%, worst cell 0.222%, worst trial K+1; 1 MB potato chunk = 268f/17.9s clean, 883f/58.9s at 70% loss; decode 1.1 us/KB; stream ceiling 2^24 packets (12.9 days @15fps) | overhead is a constant +1 packet, not a percentage - 99.69% of trials decoded at exactly K, so ADR-0004's 0.2% is a tail bound one packet wide |
| 2026-08-22 | S5 | 1 GB synthetic: peak RSS **4.6 MB** (0.0045x file), 4,096 chunks out-of-order, whole-file BLAKE3 verified, ~80 s; chunking penalty vs whole-file gzip 2.93%/2.62% at 256 KB (ADR-0006's 1-3% confirmed); probe threshold 0.95 saves 258x CPU (~12.8 s/GB) on incompressible input; resume code `P-AM2` = 5 chars (format 4-9, <=8 at 1 GB); killed at 68.8%, 22/22 retained chunks re-verified off disk, only 10 re-sent | the 1-3% figure belongs to 256 KB, not to chunking: at 64 KB the penalty is 10-11% because deflate's window is 32 KB and the chunk spends half its length warming up a dictionary it discards. Also: the inputs that dominate real transfers (mp4/jpg/zip) probe as raw, where chunk size costs nothing at all. |
| 2026-08-22 | S4 | fiducials to 0.88-1.43 px rms; clean to ±40° roll / 35° yaw / barrel k=-0.20; warped frontier webcam P8@8px 8,748 B/frame (-25.7% vs S1), potato P8@20px 1,182 (-69.9%), good P8@8px 8,748 (-59.1%) | rotation is limited by the screen leaving the sensor frame, not by the decoder — 15° roll caps framing at 68%, and lost screen area is where the throughput went. Barrel k is unobservable from 4 points; recovered from the cell grid's own between/within-cell variance ratio. A rolling-shutter tear never costs the geometry. |
